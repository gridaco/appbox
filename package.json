{
    "name": "appbox",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "app": "yarn workspace app run dev",
        "frames": "yarn workspace frames run dev"
    }
}